import React, { Component } from 'react';

import styles from './style.scss';

const Home = () => (
  <div>
    <div className={styles.testStyle}>This is the test home page</div>
    <div className={styles.imageStyle}></div>
  </div>
);

export default Home;